apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  # Uncomment when you have an Ingress controller and have set host in ingress.yaml:
  # - ingress.yaml

images:
  - name: portfolio
    newName: portfolio
    newTag: latest
